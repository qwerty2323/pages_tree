%li.page
  = link_to page.title, page
  %sup
    = link_to 'Edit', edit_page_path(page)
    = link_to 'Add Child Page', new_page_path(page)

  - if page.has_children?
    %ul.subpages= render partial:'page', collection:page.children