- if pages.size>0
  %ul.pages
    - pages.map do |page, subpages|
      %li
        = link_to page.title, page
        %sup
          = link_to 'e', edit_page_path(page)
          = link_to '+', new_page_path(page)
        = render partial:'tree', collection:subpages
