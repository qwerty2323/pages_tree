- cache @page do
  - content_for :title do
    = @page.title
  %h1
    = link_to @page.title, edit_page_path(@page)
    %sup= link_to '+', new_page_path(@page)
  .content= simple_format @page.content

  = render partial:'tree', locals:{pages:@page.cached_subtree}
